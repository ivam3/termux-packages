#!/data/data/com.termux/files/usr/bin/bash
if [[ $(id -u) -eq 0 ]]
then
  echo -en "\e[31m(_➤)\e[0m You need to be a common user"
  exit 1
else
  if [[ -e ${HOME}/.local/share/burpsuite.jar ]]
  then
		echo -en "Removing old version ...  "
		rm -rf ${HOME}/.local/share/burpsuite.jar
		echo -en "\bDone"
  fi
fi
####::: REPORT ISSUES AT https://t.me/Ivam3_Bot :::####
