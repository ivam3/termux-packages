#!/data/data/com.termux/files/usr/bin/bash
if [[ $(id -u) -eq 0 ]]
then
  echo -en "\e[31mE:\e[0m You need to be a common user"
  exit 1
elif [[ ! -e $(command -v ngrok) ]] 2>/dev/null
then
	echo -en "\e[31mE:\e[0m Ngrok needed ...\e[33m Aborting.\e[0m\n"
	exit 1
else
  echo "Preparing enviroment ..."
	[[ -d ${PREFIX}/share/nexphisher ]] || {
		mkdir -p ${PREFIX}/share/nexphisher;
	}
	[[ -d ${PREFIX}/share/nexphisher/.htr ]] || {
		mkdir ${PREFIX}/share/nexphisher/.htr;
	}
	[[ -d ${PREFIX}/share/nexphisher/logs ]] || {
		mkdir ${PREFIX}/share/nexphisher/logs;
	}
fi
####::: REPORT ISSUES AT https://t.me/Ivam3_Bot :::####
